/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['acdn.mitiendanube.com', 'www.babycottons.com.ar', 'www.rapsodia.com.ar', 'www.devre.la', 'vcp.com.ar', '*', 'imgur.com', 'i.imgur.com'],
    },
  };

export default nextConfig;
